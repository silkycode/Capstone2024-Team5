import React from 'react';

function Footer() {
    return (    
        <footer>
            <nav>
                <a href="#">Help</a>
                <a href="#">About Us</a>
                <a href="#">Contact</a>
            </nav>
        </footer>
    );
}

export default Footer;